{
  "_comment": [
    "THIS REPO'S OWN SETTINGS. Adapted from core/.claude/settings.template.json,",
    "which is payload -- a seed for the NEXT consumer, not this repo's configuration.",
    "Editing the template does not change anything here, and editing this file does",
    "not change what a consumer is seeded with.",
    "",
    "Adapted from the template as its own notes instruct:",
    "  env.DATABASE_URL      dropped -- there is no database here.",
    "  hooks.SessionStart    dropped -- it points at scripts/setup-test-db.sh,",
    "                        which this repo does not have.",
    "  permissions.deny      the drizzle-kit entries are dropped (no Drizzle here);",
    "                        Read(**/.env*) is kept, because it applies everywhere.",
    "  model                 kept as opus: this repo is almost entirely payload code.",
    "",
    "THE GUARD HOOKS POINT AT THE PAYLOAD, NOT AT A COPY. `core/.claude/guard.sh`",
    "is named directly rather than symlinked to `.claude/guard.sh`, and that is",
    "load-bearing: guard.sh locates its decision script as",
    "`dirname($BASH_SOURCE)/../scripts/guard-decision.mjs`, and $BASH_SOURCE is the",
    "path AS INVOKED, not the resolved target. Through a symlink at .claude/guard.sh",
    "that resolves to <repo>/scripts/guard-decision.mjs, which does not exist here --",
    "node would exit 1, the harness would read that as a hook error, and the tool",
    "call would PROCEED. A broken guard that fails open is worse than no guard, so",
    "the hook names the payload path where the relative lookup is correct.",
    "",
    "KNOWN GAP AT THE TIME OF WRITING, tracked as issue #11: guard-decision.mjs",
    "decides whether it was invoked directly by comparing import.meta.url against a",
    "hand-built `file://` string. Those differ when the checkout path contains a",
    "space or another URL-escaped character, and the script then exits 0 having",
    "evaluated nothing -- which guard.sh reads as ALLOW. Keep this repository's",
    "checkout on a path with no escaped characters until #11 lands.",
    "",
    "The three PreToolUse entries are not optional. They are the local half of the",
    "branch-protection story; the other half is the ruleset on main, which is in",
    "place (main is protected). Keep the longer timeout on the merge matcher -- the",
    "merge guard reads live GitHub state and 5s is not enough for it."
  ],
  "model": "opus",
  "permissions": {
    "defaultMode": "bypassPermissions",
    "allow": [
      "mcp__Claude_Code_Remote__send_later",
      "mcp__Claude_Code_Remote__create_trigger",
      "mcp__Claude_Code_Remote__update_trigger",
      "mcp__Claude_Code_Remote__list_triggers",
      "mcp__Claude_Code_Remote__delete_trigger",
      "mcp__Claude_Code_Remote__fire_trigger",
      "mcp__claude-code-remote__send_later",
      "mcp__claude-code-remote__create_trigger",
      "mcp__claude-code-remote__update_trigger",
      "mcp__claude-code-remote__list_triggers",
      "mcp__claude-code-remote__delete_trigger",
      "mcp__claude-code-remote__fire_trigger"
    ],
    "deny": [
      "Read(**/.env*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "bash core/.claude/guard.sh", "timeout": 5 }
        ]
      },
      {
        "matcher": "mcp__github__add_issue_comment|mcp__github__add_reply_to_pull_request_comment|mcp__github__pull_request_review_write",
        "hooks": [
          { "type": "command", "command": "bash core/.claude/guard.sh", "timeout": 5 }
        ]
      },
      {
        "matcher": "mcp__github__merge_pull_request",
        "hooks": [
          { "type": "command", "command": "bash core/.claude/guard.sh", "timeout": 20 }
        ]
      }
    ]
  }
}
