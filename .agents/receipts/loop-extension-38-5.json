{
  "pr": 38,
  "kind": "adjudication",
  "verdict": "ship-with-gaps-recorded",
  "grant": 0,
  "risk": "",
  "recordPath": ".agents/adjudications/38-4.json",
  "modelRequested": "best",
  "effortRequested": "xhigh",
  "decidedAt": "2026-09-06T19:49:28.000Z",
  "reasoning": "Internal tier at the David gate: budget 3, allowance 12, roundsSpent 12, trend 6,3,3,4,4,4,5,5,4,2,4,4 -- twelve passes on a 20-file/+3582 artifact that never reached zero and is flat at 4 for the last two rounds, with 48/48 findings in-diff and the last ~20 concentrated on one sub-mechanism (regex parsing of approved-plan provenance out of PR-body prose, where each fix has exposed the next Markdown-literal topology: nested headings, fences, indented/quoted, now heading-form). The 4 unresolved round-12 findings are all ordinary-consequence: a wrong planOracle on a doc PR whose body carries a blockquoted sample under a live heading (a field the plan's own decision 12b says nothing in phase 1a reads), a wrongly-blocking round-check timestamp requirement (the tier's self-announcing failure mode, with the scalar form as a workaround), a missing truncation flag on a normally-empty patch, and a 41-hex token accepted as its 40-char prefix -- none mints a false receipt, lets the merge gate pass on refused work, takes a destructive action, or widens authority, so none clears the critical bar. Two uncertainties stated rather than filled: artifact.patch was cut at 60,000 of 241,949 chars and showed me only contract/doc/test hunks, never the implementation of review-loop-record.mjs, review-budget.mjs, pr-ready.mjs or review-counting.mjs that every unresolved finding points at; and I am running on Fable 5.1 (consistent with the record's dispatch.model 'best') but the definition text I was given is the pre-#38 one that still says 'model: fable' -- exactly the checkout-vs-reviewed-head gap the record's dispatch.note names. The dispatching message's characterisation of the second David grant's stopping condition is not in the record and was not weighed.",
  "gaps": [
    "PRRT_kwDOUKOPKc6fuh_c (P1, review-loop-record.mjs:1030): a live `## Approved-plan source` heading followed by a blockquoted or indented sample provenance line still resolves the sample commit as the oracle; the live-text filter is not applied to the heading-extracted section. Recommend routing with phase 1b, the first consumer of planOracle, and reconsidering there whether regex-over-prose is the right provenance mechanism at all -- six rounds of literal-context findings (7 through 12) suggest a structured field would dissolve the class.",
    "PRRT_kwDOUKOPKc6fuh_i (P1, review-budget.mjs:2196): the round check's object-form capturedAt validation uses the default COUNTED_COLLECTIONS and so demands a reviewThreads timestamp for a collection the check never reads; a snapshot assembled exactly per pr-watch SKILL.md:104 is refused. Wrongly-blocking, self-announcing; scalar capturedAt is the workaround until fixed.",
    "PRRT_kwDOUKOPKc6fuh_f (P2, review-loop-record.mjs:1576): sinceLastReview.patch truncation is not propagated into truncation.fields, so the 'empty fields means nothing withheld' assurance can be wrong when branch movement exceeds PATCH_CAP_CHARS. That patch is empty at every write-gate dispatch, so the exposure is narrow.",
    "PRRT_kwDOUKOPKc6fuh_n (P2, review-loop-record.mjs:968): both public provenance regexes accept a 41+-hex token and capture its first 40 characters instead of refusing; needs a token boundary after the SHA.",
    "Adjudicator-side uncertainty: artifact.patch truncated at 60,000/241,949 chars; the implementation hunks the findings cite were not visible to this verdict, so their claims are taken as described, not confirmed in code.",
    "Dispatch-side uncertainty: the judge ran on Fable 5.1 under the pre-#38 definition text (`model: fable`), while the reviewed head declares `model: best` / `effort: xhigh`; the served effort is unobservable from here, as decision 9 already records."
  ],
  "operatorNote": "Three disclosures the record cannot carry. (1) As at round 8, the judge could not see the implementation: artifact.patch is capped at 60,000 chars by an approved-plan must-not-change and this artifact is 241,949. (2) The judge reports that the definition text the harness handed it was the PRE-#38 one (`model: fable`), while the record's `dispatch` -- read from the reviewed head -- says best/xhigh. This session's working directory is a consumer checkout (Overhypeme) whose synced copy of the definition predates #38, so the harness loaded that copy: the stale-checkout gap the plan records as open (decision 9, dispatch.note) has now been observed rather than only stated. The stamps record what the dispatch DECLARED at the reviewed head, which is what the field means; the served definition is disclosed here. (3) The judge's design observation -- that the provenance sub-mechanism has absorbed ~20 of 48 findings and that a structured field would dissolve the class -- is carried to David as the substantive output of this gate."
}
