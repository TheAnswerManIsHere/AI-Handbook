{
  "pr": 73,
  "kind": "adjudication",
  "verdict": "ship-with-gaps-recorded",
  "recordPath": ".agents/adjudications/73-1.json",
  "decidedAt": "2026-09-10T18:29:00Z",
  "modelRequested": "best",
  "effortRequested": "xhigh",
  "reasoning": "Internal tier at cap (declaredBudget 3, roundsSpent 3, allowance 3, no extensions), with a flat count trend of 7/6/7 across 20 findings all inside the diff (territory 20 in / 0 out) — the measured runaway shape, on an artifact whose failure mode is wrongly-blocking. Of the 7 unresolved threads, 3 are P3 documentation (a stale role description, a comment invariant, a .gitignore/README enumeration) and the 4 P2s are ordinary correctness of ordinary consequence: a usage-completeness marker parallel to the costComplete flag the loop already added, a narrowing of a boundary the contract already discloses as un-observed (spawn-time stamps), a symlinked --out path, and a permittedRoles test seam on the exported dispatch(). None reaches this rubric's critical class — nothing here deletes data, force-pushes, publishes, or touches the committed receipt/tracking machinery (fable-*.json receipts are gitignored by this same diff, and review-budget.mjs / review-loop-record.mjs / plan-review.mjs are untouched), and the two 'an operator could route around this' findings are squarely the shape the repo's own decline citation at this commit — core/.agents/memory/machinery-threat-model-is-my-own-mistakes.md, written after ten rounds of exactly this spiral on PR #7 — declines by pointing at the single-actor threat model. Two limits on this verdict, stated rather than papered over: artifact.patch was truncated at 60,000 of 100,124 chars, cutting the fable-dispatch.mjs hunks that findings at lines 657/678/808/913 point at, so I took those four findings' descriptions of the code as written and still find none critical (uncertainty cannot manufacture a critical-class flaw); and I am running as Opus 5, a permitted resolution of the declared model 'best', though I cannot independently observe the served effort level against the declared xhigh.",
  "gaps": [
    "core/scripts/fable-dispatch.mjs:913 (P2) — the --out containment check is lexical, so an in-repository path component that is a symlink to an external directory passes and the receipt is written outside the repository. Bounded consequence (one gitignored JSON file) and requires the operator to both create the symlink and name it; declined under the single-actor threat model.",
    "core/scripts/fable-dispatch.mjs:808 (P2) — no usage-completeness marker analogous to costComplete: when an attempt emits no result event its modelUsage is null, mergeUsage skips it, and the retry's usage is published as the top-level aggregate. Accounting under-report only; costUsd already refuses to present a partial sum as a total.",
    "core/scripts/fable-dispatch.mjs:678 (P2) — headAtSpawn/treeCleanAtSpawn are observed before definition lookup, schema load, nonce creation and message construction, so the stamps predate the actual launch. The contract already discloses that a tree changing during a run is not detected; this narrows an admitted boundary rather than opening a new one.",
    "core/scripts/fable-dispatch.mjs:657 (P2) — the exported dispatch() accepts permittedRoles, so a caller importing the module could widen the Phase 0 allowlist that the contract calls non-configurable. Unreachable today (PHASE0_PERMITTED_ROLES is ['probe'] and the payload ships no other role definition; adding one is itself a reviewed payload change) and nothing consumes Fable advice in Phase 0. RECOMMENDED AS THE FIRST GAP TO CLOSE IN PHASE 1, before the refusal becomes load-bearing for a real advisory role.",
    "core/scripts/fable-dispatch.mjs:57 (P3) — the header comment asserts every refusal means the dispatch did not run, but launch-surface, model-mismatch and probe-round-trip refusals are evaluated after the runner has launched and possibly billed. Comment accuracy.",
    "core/.agents/fable-roles/fable-probe.md:3 (P3) — the description still says the harness lists the probe as an ordinary subagent and that direct dispatch is possible, which the move to .agents/fable-roles/ and test R7 contradict. Stale prose whose error direction is over-cautious, not overclaiming.",
    "core/.agents/receipts/.gitignore:41 (P3) — the new fable-*.json family is a fourth ignored evidence shape while the surrounding comment still says three and .agents/receipts/README.md omits it from the file table. Documentation enumeration."
  ]
}
