# THIS REPO'S OWN review evidence, mirroring the payload copy next door in
# core/.agents/reviews/.gitignore. The reasoning -- what is ephemeral, what is
# committed instead, and why a symlink cannot work here -- lives there.
#
# scripts/check-root-wiring.mjs compares the pattern lines below against the
# payload copy and fails when they diverge. Edit the payload copy; this one
# follows.
*
!.gitignore
